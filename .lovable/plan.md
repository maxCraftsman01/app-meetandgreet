

## Make "Show all" Button More Visible

### Change
**`src/components/PropertyFinanceView.tsx` (line 272)**: Change the Button `variant` from `"ghost"` to `"outline"` to match the "Today" button style with a visible background/border.

