

## Show Simple "Wrong PIN" Message

### Change

**`src/pages/Index.tsx`** (line 92): Change the error message from `"Invalid PIN. Please try again."` to `"Wrong PIN"`.

That's it — one string change.

